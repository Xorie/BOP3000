<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    tools:context=".payment_method.Payment_method">

    <TextView
        android:id="@+id/textView4"
        android:layout_width="324dp"
        android:layout_height="53dp"
        android:text="@string/payment_method_tittle"
        android:textAllCaps="false"
        android:textColor="@color/endcolor"
        android:textSize="40sp" />

    <View
        android:id="@+id/divider12"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="?android:attr/listDivider"
        android:backgroundTint="@color/white"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pay_recview"
        android:layout_width="373dp"
        android:layout_height="171dp" />

    <Button
        android:id="@+id/btn_new_pay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/endcolor"
        android:onClick="goNewpayment"
        android:text="@string/payment_method_change"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="17sp" />

    <Button
        android:id="@+id/btn_delete_pay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/delete"
        android:text="@string/payment_method_delete"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:onClick="onClickDelete"/>

</LinearLayout>